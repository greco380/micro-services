[project]
name = "trading-system"
version = "0.1.0"
description = "Modular trading research and paper execution system"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
